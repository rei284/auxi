{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Chrome",
      "type": "pwa-chrome",
      "request": "launch",
      "file": "${workspaceFolder}/frontpage.html",
      "webRoot": "${workspaceFolder}"
    }
  ]
}